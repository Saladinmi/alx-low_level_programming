#include <stdio.h>

/**
 * main -  print name of the program 
 *@argc: int.
 *@argv: char.
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	int x;

	for (x = 0; x = argc ; x++)
	{
		printf("%s\n", argv[x]);
	}
	return (0);
}
